# Runtime deps used INSIDE the Spark container (ingestion + utilities).
# NOTE: PySpark 4.0.1 and Delta 4.0.0 are provided by the Spark image, with the
# jars baked into $SPARK_HOME/jars (see infra/spark/Dockerfile). They are deliberately
# NOT pip-installed here so we don't clobber the image's Spark. For LOCAL test runs
# they are pinned in requirements-dev.txt.
boto3>=1.34,<2
requests>=2.32,<3
trino>=0.330
# DuckDB = the default lightweight SQL consumption engine (reads Gold Delta from MinIO).
duckdb>=1.0
